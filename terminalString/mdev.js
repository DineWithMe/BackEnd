const chalk = require('chalk')
/* eslint-disable no-alert, no-console */
console.log(
  chalk.bgGreen.rgb(0, 0, 0)(
    'Development environment is up and ready, go ahead try to create and query users!'
  )
)
